/* tslint:disable */
import {
  UserUserIdSubscribitions,
} from '../models';

export function isUserUserIdSubscribitions(arg: any): arg is UserUserIdSubscribitions {
  return (
    arg != null &&
    typeof arg === 'object' &&

    true
  );
}

